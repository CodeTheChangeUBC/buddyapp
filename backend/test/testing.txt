To run the tests:
1. Go to /buddyapp/backend in terminal (cd ~/buddyapp/backend if you have the repo in your home directory)
2. Run: npm install -g mocha
3. To run "ScoreSpec.js," run: mocha ScoreSpec.js
